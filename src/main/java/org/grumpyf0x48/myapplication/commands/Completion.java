package org.grumpyf0x48.myapplication.commands;

// This class is processed by Picocli Codegen
// which needs a no-args constructor in Command class
// to generate Bash completion script
// That cannot be done in super class because of Spring dependency injection

public class Completion extends Command {

    public Completion() {
        super(null);
    }
}
